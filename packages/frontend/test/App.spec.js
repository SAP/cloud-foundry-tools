// import App from "../src/App";
// let wrapper;

describe("App.vue", () => {
  test("component name", () => {
    // wrapper = initComponent(App);
    // expect(wrapper.name()).toBe("App");
    expect(true).toBe(true);
  });
});
